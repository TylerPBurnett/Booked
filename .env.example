# Required for AI tag/category classification (optional — bookmarks still import without it)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Override Chrome user data path (macOS default used if not set)
# CHROME_USER_DATA=/Users/YOUR_NAME/Library/Application Support/Google/Chrome

# Server port (default: 3333)
PORT=3333
